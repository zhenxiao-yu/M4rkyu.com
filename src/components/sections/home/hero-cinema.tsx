// Intentionally empty — the standalone "values cinema" was removed in favor of
// the home-spine scroll redesign. File deletion is blocked by sandbox
// permissions in this environment, so this stub stands in until it can be
// removed from disk. It is not imported anywhere.
export {};
